import 'package:flutter/material.dart';

class RoleManage extends StatelessWidget {
  const RoleManage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
